#include "monty.h"

/**
 * o_stack - data set to stack mode (LIFO)
 * @stack: pointer to the stack
 * @counter: opcode line number
 * Return: void
*/
void o_stack(stack_t **stack, unsigned int counter)
{
	(void)stack;
	(void)counter;
	var.storque = 0;
}
